<UserControl
    x:Class="Paint_Keyboard.Controls.ItemView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" 
    d:DesignHeight="70" 
    d:DesignWidth="70"
    Width="70"
    Height="70" 
    IsHitTestVisible="False"
    >

    <Border x:Name="Border" BorderThickness="3" BorderBrush="Black" Background="Black">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock x:Name="GlyphTextBlock" Foreground="White" Margin="6" FontSize="18" FontFamily="Segoe UI Symbol" />
            <TextBlock x:Name="TitleTextBlock" Foreground="White"/>
        </StackPanel>
    </Border>

</UserControl>